/**
 * Customer Repository Placeholder
 * Database access layer (PostgreSQL / Knex / Prisma / TypeORM / Raw SQL).
 */

class CustomerRepository {
  async findMany() {
    /* To be implemented by database / backend engineer */
    return [];
  }

  async findOne(id) {
    /* To be implemented by database / backend engineer */
    return null;
  }
}

module.exports = new CustomerRepository();
