/**
 * Shared Constants — Used by both Frontend and Backend
 */
const PLAN_TIERS = ['Starter', 'Professional', 'Enterprise'];
const CUSTOMER_STATUSES = ['Active', 'Trial', 'Suspended', 'Expired'];
const PLATFORM_ROLES = ['Super Admin', 'Support Lead', 'Security Auditor', 'Billing Manager'];
const AUDIT_SEVERITIES = ['Info', 'Warning', 'Critical'];

module.exports = { PLAN_TIERS, CUSTOMER_STATUSES, PLATFORM_ROLES, AUDIT_SEVERITIES };
